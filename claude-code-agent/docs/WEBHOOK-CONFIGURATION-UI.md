# Webhook Dashboard Configuration UI

## Overview

**All webhook settings are fully configurable via the dashboard:**
- ✅ Edit commands (triggers, actions, templates)
- ✅ Configure bot mention tags (@agent, @ai-assistant, etc.)
- ✅ Set trigger patterns (mention, assignee, labels, status)
- ✅ Customize response templates
- ✅ Enable/disable commands individually
- ✅ Test webhooks before deployment

---

## Dashboard UI Components

### **1. Webhook Configuration Editor**

```
┌─────────────────────────────────────────────────────────┐
│ 📡 Webhook: GitHub Issue Tracker                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ Basic Settings                                          │
│ ┌─────────────────────────────────────────────────┐   │
│ │ Name: [GitHub Issue Tracker              ]     │   │
│ │ Provider: [GitHub ▼]                            │   │
│ │ Enabled: [✓] Active                             │   │
│ │ Endpoint: /webhooks/github/webhook-abc123       │   │
│ │ Secret: [••••••••••] [Regenerate]               │   │
│ └─────────────────────────────────────────────────┘   │
│                                                         │
│ Bot Configuration                                       │
│ ┌─────────────────────────────────────────────────┐   │
│ │ Mention Tags (comma-separated):                 │   │
│ │ [@agent, @ai-assistant, @bot]                   │   │
│ │                                                  │   │
│ │ Assignee Triggers:                               │   │
│ │ [✓] Trigger when assigned to: AI Agent          │   │
│ │ [✓] Trigger when assigned to: automation-bot    │   │
│ └─────────────────────────────────────────────────┘   │
│                                                         │
│ Commands (3)                                            │
│ ┌─────────────────────────────────────────────────┐   │
│ │ 1. Issue Comment with @mention                  │   │
│ │    Trigger: issue_comment.created               │   │
│ │    Condition: body contains @agent              │   │
│ │    Action: create_task → planning               │   │
│ │    [Edit] [Disable] [Delete]                    │   │
│ ├─────────────────────────────────────────────────┤   │
│ │ 2. Issue Assigned to Bot                        │   │
│ │    Trigger: issues.assigned                     │   │
│ │    Condition: assignee == "AI Agent"            │   │
│ │    Action: ask → brain                          │   │
│ │    [Edit] [Disable] [Delete]                    │   │
│ ├─────────────────────────────────────────────────┤   │
│ │ 3. Urgent Label Added                           │   │
│ │    Trigger: issues.labeled                      │   │
│ │    Condition: label == "urgent"                 │   │
│ │    Action: create_task → planning               │   │
│ │    [Edit] [Disable] [Delete]                    │   │
│ └─────────────────────────────────────────────────┘   │
│                                                         │
│ [+ Add Command] [Test Webhook] [Save] [Delete]         │
└─────────────────────────────────────────────────────────┘
```

### **2. Command Editor Modal**

```
┌─────────────────────────────────────────────────────────┐
│ Edit Command: Issue Comment with @mention              │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ Trigger Configuration                                   │
│ ┌─────────────────────────────────────────────────┐   │
│ │ Event Type: [issue_comment.created ▼]          │   │
│ │                                                  │   │
│ │ Trigger Conditions (all must match):            │   │
│ │ ┌──────────────────────────────────────────┐   │   │
│ │ │ Field: [body ▼]                          │   │   │
│ │ │ Operator: [contains ▼]                   │   │   │
│ │ │ Value: [@agent]                          │   │   │
│ │ │ [Remove]                                  │   │   │
│ │ └──────────────────────────────────────────┘   │   │
│ │ [+ Add Condition]                                │   │
│ └─────────────────────────────────────────────────┘   │
│                                                         │
│ Action Configuration                                    │
│ ┌─────────────────────────────────────────────────┐   │
│ │ Action Type: [create_task ▼]                    │   │
│ │ Assign to Agent: [planning ▼]                   │   │
│ │                                                  │   │
│ │ Message Template:                                │   │
│ │ ┌──────────────────────────────────────────┐   │   │
│ │ │ User {{comment.user.login}} asked:       │   │   │
│ │ │ {{comment.body}}                          │   │   │
│ │ │                                            │   │   │
│ │ │ Issue: {{issue.title}} (#{{issue.number}})│  │   │
│ │ └──────────────────────────────────────────┘   │   │
│ │                                                  │   │
│ │ Available Variables:                             │   │
│ │ {{comment.body}}, {{comment.user.login}},       │   │
│ │ {{issue.number}}, {{issue.title}}, {{issue.url}}│   │
│ └─────────────────────────────────────────────────┘   │
│                                                         │
│ Response Configuration (optional)                       │
│ ┌─────────────────────────────────────────────────┐   │
│ │ [✓] Post comment back to issue                  │   │
│ │ Response Template:                               │   │
│ │ ┌──────────────────────────────────────────┐   │   │
│ │ │ 🤖 I've created task {{task_id}} to      │   │   │
│ │ │ handle this. I'll update you shortly.    │   │   │
│ │ └──────────────────────────────────────────┘   │   │
│ └─────────────────────────────────────────────────┘   │
│                                                         │
│ Priority & Status                                       │
│ ┌─────────────────────────────────────────────────┐   │
│ │ Priority: [0 (highest) ▼]                       │   │
│ │ Enabled: [✓] Active                             │   │
│ └─────────────────────────────────────────────────┘   │
│                                                         │
│ [Test Command] [Save] [Cancel]                         │
└─────────────────────────────────────────────────────────┘
```

---

## Configurable Trigger Patterns

### **1. Mention Patterns**

**Dashboard Configuration:**
```
Bot Mention Tags:
┌────────────────────────────────────┐
│ @agent                             │
│ @ai-assistant                      │
│ @automation-bot                    │
│ @claude                            │
│                                    │
│ [+ Add Tag]                        │
└────────────────────────────────────┘

Match Type:
( ) Exact match only
(•) Case-insensitive
( ) Regex pattern
```

**Examples:**
- `@agent please review this PR`
- `Hey @ai-assistant, can you help?`
- `Assigning to @automation-bot`

### **2. Assignee Triggers**

**Dashboard Configuration:**
```
Assignee Triggers:
┌────────────────────────────────────┐
│ [✓] AI Agent                       │
│ [✓] automation-bot                 │
│ [✓] claude-assistant               │
│ [ ] bot-user                       │
│                                    │
│ [+ Add Assignee]                   │
└────────────────────────────────────┘

Trigger When:
[✓] Issue assigned to bot
[✓] PR assigned to bot
[ ] Comment mentions assignee
```

**Example Jira Configuration:**
```
Jira Assignee Triggers:
┌────────────────────────────────────┐
│ When assignee changes to:          │
│ [✓] AI Agent                       │
│ [✓] automation                     │
│                                    │
│ Action: [create_task ▼]            │
│ Agent: [planning ▼]                │
│                                    │
│ Auto-comment:                      │
│ [✓] "I've been assigned to this    │
│     ticket and will analyze it."   │
└────────────────────────────────────┘
```

### **3. Label/Tag Triggers**

**Dashboard Configuration:**
```
Label Triggers:
┌────────────────────────────────────┐
│ Label: [urgent        ]            │
│ Action: [create_task ▼]            │
│ Agent: [planning ▼]                │
│ Priority: [High ▼]                 │
│ [Add]                              │
├────────────────────────────────────┤
│ Active Triggers:                   │
│ • urgent → planning (high)         │
│ • bug → executor (normal)          │
│ • needs-review → brain (ask)       │
└────────────────────────────────────┘
```

### **4. Status Change Triggers**

**Dashboard Configuration:**
```
Status Change Triggers:
┌────────────────────────────────────┐
│ From: [Any ▼]                      │
│ To: [In Progress ▼]                │
│ Action: [comment ▼]                │
│ Message: "I'll monitor this"       │
│ [Add]                              │
├────────────────────────────────────┤
│ Active Triggers:                   │
│ • → In Progress: comment           │
│ • → Done: create_task (verify)     │
│ • → Blocked: ask (clarification)   │
└────────────────────────────────────┘
```

---

## Command Types & Configuration

### **1. Create Task Command**

```
Action: create_task
┌────────────────────────────────────┐
│ Assign to Agent: [planning ▼]     │
│                                    │
│ Task Template:                     │
│ ┌──────────────────────────────┐  │
│ │ {{event.type}}: {{title}}    │  │
│ │ {{description}}              │  │
│ │                              │  │
│ │ Priority: {{priority}}       │  │
│ └──────────────────────────────┘  │
│                                    │
│ Task Priority:                     │
│ ( ) Use webhook priority           │
│ (•) Use condition-based priority   │
│ ( ) Always high                    │
└────────────────────────────────────┘
```

### **2. Comment Command**

```
Action: comment
┌────────────────────────────────────┐
│ Comment Template:                  │
│ ┌──────────────────────────────┐  │
│ │ 🤖 Acknowledged!             │  │
│ │                              │  │
│ │ I've received this {{type}}  │  │
│ │ and will process it.         │  │
│ └──────────────────────────────┘  │
│                                    │
│ Comment Options:                   │
│ [✓] Use bot account                │
│ [✓] Add reaction emoji             │
│ [ ] Mark as resolved               │
└────────────────────────────────────┘
```

### **3. Ask Command (Interactive)**

```
Action: ask
┌────────────────────────────────────┐
│ Assign to Agent: [brain ▼]        │
│                                    │
│ Question Template:                 │
│ ┌──────────────────────────────┐  │
│ │ I see {{event.type}}.        │  │
│ │                              │  │
│ │ Should I:                    │  │
│ │ 1. Create a task             │  │
│ │ 2. Comment only              │  │
│ │ 3. Ignore                    │  │
│ └──────────────────────────────┘  │
│                                    │
│ Response Handling:                 │
│ [✓] Wait for user response         │
│ [✓] Timeout after: [30] minutes    │
│ [ ] Auto-proceed if no response    │
└────────────────────────────────────┘
```

### **4. Respond Command (Immediate)**

```
Action: respond
┌────────────────────────────────────┐
│ Response Template:                 │
│ ┌──────────────────────────────┐  │
│ │ Received and processing      │  │
│ │ {{event.type}}.              │  │
│ │                              │  │
│ │ Task ID: {{task_id}}         │  │
│ └──────────────────────────────┘  │
│                                    │
│ Response Method:                   │
│ (•) Comment on source              │
│ ( ) Send webhook response          │
│ ( ) Both                           │
└────────────────────────────────────┘
```

---

## Provider-Specific Configurations

### **GitHub Webhook Configuration**

```
GitHub Settings
┌────────────────────────────────────┐
│ Repository: [org/repo]             │
│ Events to Monitor:                 │
│ [✓] Issues                         │
│ [✓] Pull Requests                  │
│ [✓] Issue Comments                 │
│ [✓] PR Comments                    │
│ [ ] Releases                       │
│ [ ] Pushes                         │
│                                    │
│ Bot Account: [@ai-agent]           │
│ Mention Triggers: [@agent, @bot]   │
│                                    │
│ Auto-label Processed Items:        │
│ [✓] Add "bot-processed" label      │
└────────────────────────────────────┘
```

### **Jira Webhook Configuration**

```
Jira Settings
┌────────────────────────────────────┐
│ Project: [PROJ ▼]                  │
│ Issue Types:                       │
│ [✓] Bug                            │
│ [✓] Task                           │
│ [✓] Story                          │
│ [ ] Epic                           │
│                                    │
│ Bot User: [ai-agent@company.com]   │
│                                    │
│ Assignee Triggers:                 │
│ [✓] AI Agent                       │
│ [✓] Automation                     │
│                                    │
│ Mention Triggers:                  │
│ [@agent, @ai, @automation]         │
│                                    │
│ Auto-transition:                   │
│ [✓] Move to "In Progress" when     │
│     task created                   │
└────────────────────────────────────┘
```

### **Slack Webhook Configuration**

```
Slack Settings
┌────────────────────────────────────┐
│ Workspace: [company.slack.com]     │
│ Channels to Monitor:               │
│ [✓] #engineering                   │
│ [✓] #support                       │
│ [ ] #general                       │
│                                    │
│ Bot User: [@ai-assistant]          │
│                                    │
│ Mention Triggers:                  │
│ [@ai-assistant, @bot, @agent]      │
│                                    │
│ Direct Message Handling:           │
│ (•) Always respond                 │
│ ( ) Only if mentioned              │
│ ( ) Ignore DMs                     │
│                                    │
│ Thread Behavior:                   │
│ [✓] Reply in thread                │
│ [ ] Reply in channel               │
└────────────────────────────────────┘
```

---

## Testing Webhooks

### **Test Panel**

```
┌─────────────────────────────────────────────────────────┐
│ 🧪 Test Webhook: GitHub Issue Tracker                  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ Test Event Type: [issue_comment.created ▼]             │
│                                                         │
│ Sample Payload:                                         │
│ ┌─────────────────────────────────────────────────┐   │
│ │ {                                                │   │
│ │   "action": "created",                           │   │
│ │   "issue": {                                     │   │
│ │     "number": 123,                               │   │
│ │     "title": "Test Issue"                        │   │
│ │   },                                             │   │
│ │   "comment": {                                   │   │
│ │     "body": "@agent please help with this",     │   │
│ │     "user": {"login": "testuser"}                │   │
│ │   }                                              │   │
│ │ }                                                │   │
│ └─────────────────────────────────────────────────┘   │
│                                                         │
│ [Load Sample] [Send Test Event]                        │
│                                                         │
│ Test Results:                                           │
│ ┌─────────────────────────────────────────────────┐   │
│ │ ✓ Webhook received                               │   │
│ │ ✓ Signature verified                             │   │
│ │ ✓ Matched command: "Issue Comment with @mention"│   │
│ │ ✓ Created task: task-abc123                     │   │
│ │ ✓ Posted comment: "I've created task..."        │   │
│ │                                                  │   │
│ │ Execution time: 234ms                            │   │
│ └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

---

## Brain Agent Configuration Knowledge

### **Brain Knows How to Configure Webhooks**

The Brain agent has **full knowledge** of webhook configuration:

**User asks:**
```
"Set up a Jira webhook that triggers when issues are assigned to 'AI Agent'"
```

**Brain responds:**
```
I'll configure a Jira webhook with assignee triggers.

Creating webhook with:
- Provider: Jira
- Assignee triggers: ["AI Agent"]
- Command: When assigned → create planning task
- Auto-comment: "I've been assigned and will analyze this ticket"

Webhook created: /webhooks/jira/jira-assignee-001
```

**Brain can also:**
- Edit existing commands
- Add new trigger patterns
- Modify bot mention tags
- Update response templates
- Enable/disable commands
- Test webhooks

---

## API Endpoints for Configuration

```python
# Edit command
PUT /api/webhooks/{webhook_id}/commands/{command_id}
{
  "trigger": "issues.assigned",
  "conditions": {
    "assignee": "AI Agent"
  },
  "action": "create_task",
  "agent": "planning",
  "template": "Assigned issue: {{issue.title}}"
}

# Update bot tags
PUT /api/webhooks/{webhook_id}/bot-config
{
  "mention_tags": ["@agent", "@ai-assistant", "@bot"],
  "assignee_triggers": ["AI Agent", "automation-bot"]
}

# Test webhook
POST /api/webhooks/{webhook_id}/test
{
  "event_type": "issue_comment.created",
  "payload": {...}
}
```

---

## Summary

**Everything is configurable via dashboard:**
- ✅ Commands (add, edit, delete)
- ✅ Triggers (mention, assignee, labels, status)
- ✅ Bot tags (@agent, @ai-assistant, etc.)
- ✅ Response templates
- ✅ Provider-specific settings
- ✅ Test before deployment

**Brain agent can:**
- ✅ Configure all settings via API
- ✅ Understand user requirements
- ✅ Create optimal configurations
- ✅ Test and validate
- ✅ Monitor and adjust

**No code required - all via UI or Brain commands!**
