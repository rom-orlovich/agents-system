[project]
name = "jira-mcp-server"
version = "0.1.0"
description = "Jira MCP server using shared jira-client"
requires-python = ">=3.11"
dependencies = [
    "mcp>=0.1.0",
    "jira-client",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
jira-client = { path = "../jira_client", editable = true }

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
