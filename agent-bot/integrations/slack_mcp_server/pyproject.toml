[project]
name = "slack-mcp-server"
version = "0.1.0"
description = "Slack MCP server using shared slack-client"
requires-python = ">=3.11"
dependencies = [
    "mcp>=0.1.0",
    "slack-client",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
slack-client = { path = "../slack_client", editable = true }

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
